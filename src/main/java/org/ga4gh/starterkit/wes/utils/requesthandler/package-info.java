/**
 * Contains classes that harbor the main logic for controller functions, abstracting
 * it away from the 'controller' package. Request handlers are ephemeral and loaded
 * on a per-request basis
 */
package org.ga4gh.starterkit.wes.utils.requesthandler;
